# ANOMALIES.md — что не так с `review.csv`

`review.csv` подавался как «свежая выгрузка для той же базы». По факту это **грязный дамп с подмешанными старыми записями и множеством битых значений**. Ниже — всё странное, что нашлось, и **как именно** это обнаружено.

**Как проверял (метод):**
1. Распарсил CSV собственным парсером (RFC 4180, с учётом кавычек) — `scripts/csv.mjs`.
2. Загрузил сырьё «как есть» в staging-таблицу `reviews_raw` (все колонки `TEXT`), чтобы битые значения не рушили импорт.
3. Прогнал валидацию каждого поля регулярками/парсерами и сверил `id` с уже загруженной базой из задачи 1 (`scripts/load_reviews.mjs`).
4. Отдельно проверил сырые байты файла (`cat -A`) — переносы строк и пустые строки.

Итого строк данных: **207** (207 непустых записей CSV после заголовка).

---

## 1. Главный «сюрприз»: под видом свежих данных — старые записи
6 строк ссылаются на `id`, которые **уже есть** в базе из задачи 1 (диапазон `c_000xxx`), а не на новый диапазон `c_001xxx`:

`c_000098, c_000246, c_000263, c_000425, c_000769, c_000851`

Поля в них совпадают с уже загруженными компаниями — то есть это **не обновления, а дубли ранее выгруженного**. «Свежей» выгрузка не является.
**Как нашёл:** пересечение множества `id` из CSV с `SELECT id FROM companies`.
**Что сделал:** при мёрже эти строки пропущены (`ON CONFLICT DO NOTHING` + явная проверка), чтобы не плодить дубли.

## 2. Две полностью пустые строки
В конце файла две строки вида `,,,,,,,,` (только запятые, все поля пустые), строки 207–208.
**Как нашёл:** `tail -4 review.csv | cat -A` показал `,,,,,,,,^M$`; в коде — строки, где все поля пустые.
**Что сделал:** отброшены как мусор.

## 3. Дубликаты `id` внутри самого файла
Три `id` встречаются дважды (записи идентичны): `c_001049`, `c_001050`, `c_001075`.
**Как нашёл:** подсчёт частот `id` среди непустых строк.
**Что сделал:** в базу берётся один экземпляр (дедупликация по `id`).

## 4. Битые значения `rating`
| id | значение | проблема |
|----|----------|----------|
| `c_001083` | `N/A` | не число |
| `c_001122` | `-3` | отрицательный, вне диапазона 0–5 |
| `c_001186` | `7.2` | больше 5, вне диапазона |
| `c_001010` | `4,5` | десятичный разделитель — запятая вместо точки |

`4,5` — валидное значение (4.5), просто в «европейском» формате: при загрузке нормализуется (`,`→`.`). Остальные три — реально невалидны и записаны как `NULL`.
Плюс ~20 строк с пустым `rating` (допустимо — как и `NULL` в исходной выгрузке).
**Как нашёл:** `parseRating()` — регулярка на число + проверка диапазона 0–5.

## 5. Битые значения `reviews_count`
| id | значение | проблема |
|----|----------|----------|
| `c_001116` | `-10` | отрицательное число отзывов |
| `c_001079` | `45.5` | дробное, а должно быть целое |
| `c_001187` | `много` | текст вместо числа |

**Как нашёл:** `parseReviews()` — только целое ≥ 0 считается валидным.
**Что сделал:** невалидные → `0` (поле в схеме `NOT NULL DEFAULT 0 CHECK >= 0`).

## 6. Битые значения `site`
| id | значение | проблема |
|----|----------|----------|
| `c_001132` | `https://` | схема без домена |
| `c_001064` | `нет сайта` | текст-заглушка вместо URL |
| `c_001020` | `htp://sintez-service-453.ru` | опечатка в схеме (`htp` вместо `http`) |

Плюс ~60 строк с пустым `site`.
**Как нашёл:** проверка URL регуляркой `^https?://…\.…$`.
**Что сделал:** невалидные → `NULL`.

## 7. Битые значения `phone`
| id | значение | проблема |
|----|----------|----------|
| `c_001004` | `8 (925) abc-12-34` | буквы вместо цифр, префикс `8` вместо `+7` |
| `c_001091` | `+7` | обрезанный номер |

Плюс ~22 строки с пустым `phone`.
**Как нашёл:** маска `^\+7 \(\d{3}\) \d{3}-\d{2}-\d{2}$`.
**Что сделал:** невалидные → `NULL`.

## 8. Лишние пробелы
`c_001184` — город `"Москва "` с хвостовым пробелом (без чистки попал бы отдельным городом от `Москва`).
**Как нашёл:** сравнение `value !== value.trim()`.
**Что сделал:** `TRIM` при загрузке.

## 9. «Чужой» диапазон id `c_9000xx`
6 записей имеют id из совсем другого диапазона — `c_900006`, `c_900007`, `c_900008`, `c_900009`, `c_900010`, `c_900011` (резкий скачок от последовательного `c_001xxx`). У всех шести ещё и **названия без кавычек-ёлочек** «...», которые используются во всех остальных записях: `АО Флагман Лаб`, `АО Сокол`, `Модуль Строй`, `АО Сокол Лаб`, `Прайм Плюс`, `АО Орион Групп`. Похоже на данные из другой партии/источника, подмешанные в файл.
**Как нашёл:** разложил числовую часть id по диапазонам и заметил отдельную группу ~900000; параллельно — записи без символов `«»`.
**Что сделал:** формат id валиден (`c_` + 6 цифр) и в базе их нет → загрузил как новые компании, но зафиксировал как аномалию источника.

## 10. Экранированные кавычки и название-заглушка
`c_001145` — в CSV имя записано как `"ООО ""Ромашка"""` (RFC 4180: `""` = одна кавычка `"`), т.е. `ООО "Ромашка"` с прямыми ASCII-кавычками вместо ёлочек. Плюс само «Ромашка» — типовое placeholder-название.
**Как нашёл:** мой CSV-парсер корректно разворачивает `""`; строка выделилась и прямыми кавычками, и «тестовым» именем.
**Что сделал:** значение валидно — загрузил как есть (проверка на то, что парсер правильно обрабатывает экранирование кавычек).

## 11. Битая кодировка (мохибейк) в `name` и `city`
Две записи содержат текст в «двойной» кодировке (UTF-8 прочитан как Windows-1251 и снова сохранён как UTF-8):
| id | как в файле | должно быть |
|----|-------------|-------------|
| `c_001008` | `РћРћРћ В«Р—Р°СЂСЏ РўРµС…В»` / `РњРѕСЃРєРІР°` | `ООО «Заря Тех»` / `Москва` |
| `c_001128` | `РћРћРћ В«РўРµРјРї РњРµРґРёР°В»` / `РЎР°РЅРєС‚-РџРµС‚РµСЂР±СѓСЂРі` | `ООО «Темп Медиа»` / `Санкт-Петербург` |

**Как нашёл:** значения не входили в эталонный набор городов; характерный признак — цепочки `Р…`/`С…`/`В«`.
**Что сделал:** восстановил исходный текст обратным преобразованием CP1251→UTF-8 (`scripts/clean.mjs`, функция `repairMojibake`); после починки `city` совпал с эталонным городом.

## 12. Разнобой в написании города
Один и тот же город записан по-разному, из-за чего в фильтре плодились дубли:
| id | в файле | нормализовано к |
|----|---------|-----------------|
| `c_001108` | `Moscow` (латиница) | `Москва` |
| `c_001047` | `москва` (со строчной) | `Москва` |
| `c_001182` | `Санкат-Петербург` (опечатка) | `Санкт-Петербург` |
| `c_001184` | `Москва ` (хвостовой пробел) | `Москва` |

**Как нашёл:** сравнил города из `review.csv` с эталонным набором из основной выгрузки (20 городов) — всё, что не совпало.
**Что сделал:** нормализация к эталону: регистр/пробелы, таблица алиасов (`moscow`→`Москва`), нечёткое совпадение (расстояние Левенштейна ≤ 2 ловит опечатку `Санкат`→`Санкт`). Так фильтр по городу снова показывает ровно 20 чистых значений.

## 13. Сдвиг колонок (пропущена `category`)
`c_001015`: в строке пропущено значение `category`, из-за чего всё съехало — город `Пермь` попал в колонку категории, а адрес `ул. Советская, д. 89, офис 43` — в колонку города:
`c_001015,АО «Платформа»,Пермь,"ул. Советская, д. 89, офис 43",,4.2,77,,+7 (383) 920-78-13`
**Как нашёл:** город не распознался (это адрес), но значение из `category` (`Пермь`) оказалось валидным **городом** — верный признак сдвига на одну колонку.
**Что сделал:** восстановил выравнивание — `city = Пермь` (из category), `address =` исходный адрес, `category = NULL` (реальное значение потеряно в источнике). `rating/reviews_count/phone` были на своих местах.

---

## Сводка обработки при мёрже в `companies`
- Новых валидных компаний добавлено: **196** (190 из диапазона `c_001xxx` + 6 из «чужого» `c_9000xx`).
- Из них с очищенными/нормализованными полями: **17** (битые значения → `NULL`/`0`, нормализация города, починка кодировки, выравнивание сдвига).
- Пропущено как дубли уже существующих: **6**.
- Пропущено как дубли внутри файла: **3**.
- Пустых строк отброшено: **2**.
- Итог в таблице: `994` (задача 1) + `196` = **1190** компаний.

## Прочее наблюдение (не по данным, а по ТЗ)
В критериях оценивания упомянут пункт «реальные, валидные **email**», но **в датасете колонки `email` нет вовсе** — есть только `site` и `phone`. Похоже, критерий частично унаследован от другой версии задания. Ближайшие контактные поля (`site`, `phone`) я провалидировал (см. пункты 6–7).
